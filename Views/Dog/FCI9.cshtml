@model IEnumerable<Dog>
@{
    ViewData["Title"] = "FCI9";
}

<h1>Группа FCI 9 - </h1>
<p>
</p>
<h1>Все породы FCI Group 9</h1>
<div class="row">
    @if (Model != null && Model.Count() > 0)
    {
        @foreach (var item in Model)
        {
            @if (item.FCIGroup == "9")
            {
                <partial name="_DogCardPartial" model="@item"></partial>
            }
        }
    }

</div>
