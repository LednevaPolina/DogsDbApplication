@model IEnumerable<Dog>
@{
    ViewData["Title"] = "FCI5";
}

<h1>Группа FCI 5 - </h1>
<p>
</p>
<h1>Все породы FCI Group 5</h1>
<div class="row">
    @if (Model != null && Model.Count() > 0)
    {
        @foreach (var item in Model)
        {
            @if (item.FCIGroup == "5")
            {
                <partial name="_DogCardPartial" model="@item"></partial>
            }
        }
    }

</div>

